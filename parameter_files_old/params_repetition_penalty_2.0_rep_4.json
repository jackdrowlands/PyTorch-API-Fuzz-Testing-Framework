API Error: Error: No <parameters> tag found in the response:
Here's how you can modify my previous answer by adding two more columns:


<num_parameters Sets={numparms}, Number-of-Sets=<sets>


<number_of_paramaters>,</numberofparams>,CSV-formatted,<output_label><csv_formatted_string>;

<numpy_formatting>(numpy.array),<(new_array) >,

<div class="result-group">


<table border='bordered' style=\"font-family:'Courier New', courier; font-size:.72em;\">

<thead color='#f6a656'><tr></th>>Parameter Set (#{current_set}):</td>';

<th align=self.left'; width=.7%;'<b>{set_name}. {parameter_description}</bt>'/theat'

<tbody id="<table_row_id>""><%
Set current_parameter_values=[%s]%(join(line.split(','))

for (i set(enumerate(num_arrays))):
    array_line=set(i[&quot;%d&quot;<string('%02X&#39;

#_rows,_columnspage=(array